{
  "name": "lib",
  "version": "0.0.0",
  "scripts": {
    "babel-watch": "babel src --watch --out-dir cjs --delete-dir-on-start --minified",
    "build": "babel src -d cjs --delete-dir-on-start --minified"
  },
  "devDependencies": {
    "@babel/cli": "^7.21.5",
    "@babel/core": "^7.21.8",
    "@babel/preset-env": "^7.21.5",
    "concurrently": "^8.0.1"
  }
}
